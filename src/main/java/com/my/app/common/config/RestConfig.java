package com.my.app.common.config;

//@Configuration
public class RestConfig {

	/*
	 * @Bean public RestTemplate restTemplate() throws Exception { SSLContext
	 * sslContext = SSLContexts.custom().loadTrustMaterial(new
	 * TrustAllStrategy()).build();
	 * 
	 * SSLConnectionSocketFactory sslConnectionSocketFactory = new
	 * SSLConnectionSocketFactory(sslContext);
	 * 
	 * CloseableHttpClient httpClient =
	 * HttpClients.custom().setSSLSocketFactory(sslConnectionSocketFactory).build();
	 * 
	 * HttpComponentsClientHttpRequestFactory requestFactory = new
	 * HttpComponentsClientHttpRequestFactory();
	 * requestFactory.setHttpClient(httpClient);
	 * 
	 * RestTemplate restTemplate = new RestTemplate();
	 * restTemplate.setRequestFactory(requestFactory); return restTemplate; }
	 */

}
